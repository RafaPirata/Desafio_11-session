const validacionDato = (title, price, thumbnail) => {
  // if (!title || !price || !thumbnail) return { error: "Error validacion" };
  // else
  return { title, price, thumbnail };
};
module.exports = validacionDato;
